language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-woocommerce-payment-gateway' ]; then cd .. && eval "mv $currentfolder generator-woocommerce-payment-gateway" && cd generator-woocommerce-payment-gateway; fi

